@model IEnumerable<ArtRoyalDetailing.Models.Service>

@{
    ViewData["Title"] = "Services";
}

<h1>Services</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ServiceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ServiceTypeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceTypeNavigation.TypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdService">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdService">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdService">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
